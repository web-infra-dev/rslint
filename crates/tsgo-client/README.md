# tsgo-client

A Rust client library for communicating with TypeScript Go (tsgo) processes to load and analyze TypeScript projects.

## Overview

`tsgo-client` provides a high-level API for spawning and communicating with a TypeScript Go executable.
