import TypeScriptVersions from '@/theme/components/TypeScriptVersions';

# Bundled TypeScript Versions

TypeScript releases and commits bundled with each Rslint version.

<TypeScriptVersions />

A TypeScript release is shown only when its tag points to the exact recorded
commit. Other revisions are identified by their commit hash.

TypeScript's Go compiler moved from the archived
[microsoft/typescript-go](https://github.com/microsoft/typescript-go) repository
to [microsoft/TypeScript](https://github.com/microsoft/TypeScript). We therefore
track bundled TypeScript versions from **Rslint v0.9.2** onward,
without backfilling **v0.9.1 and earlier**.
